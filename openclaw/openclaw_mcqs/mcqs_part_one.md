MCQ 1: Which of the following describes capabilities and control?

1. Capabilities are Ownership, Always-on, Proactive, Extensible, Multi-agent and Control is Proactive.

2. Controllers are Multi-channel, Always-on, Proactive, Extensible, Multi-agent and Capability is Ownership.

3. Capabilities are Multi-channel, Ownership, Proactive, Extensible, Multi-agent and Control is Extensibility.

4. Capabilities are Multi-channel, Always-on, Proactive, Extensible, Multi-agent and Control is Ownership.

MCQ 2: What does Ownership defines?

1. Ownership means the agent stays inside your laptop.

2. Ownership means you can ask the agent from any channel like Telegram, WhatsApp etc...

3. Ownership means the agent stays inside your machine, runs 24/7 and can delegate tasks to other agents when required.

4. Ownership means you have the control.

MCQ 3: What is the best match based on the book content?

1. Workspace = Kernel, Plugins = Firmware, Sessions = Device drivers, Workspace files = Process memory.

2. Sessions = Kernel, Plugins = Firmware, Workspace files = Device drivers, Gateway = Process memory.

3. Gateway = Kernel, Workspace files = Firmware, Plugins = Device drivers, Sessions = Process memory.

4. Plugins = Kernel, Sessions = Firmware, Gateway = Device drivers, Workspace files = Process memory.

MCQ 4: What are the five dimensions of an AI Employee?

1. Multi-channel, Always-on, Proactive, Extensible, Ownership

2. Multi-channel, Always-on, Ownership, Extensible, Multi-agent

3. Multi-channel, Always-on, Proactive, Extensible, Multi-agent

4. Multi-channel, Ownership, Proactive, Extensible, Multi-agent

MCQ 5: What is the difference between capabilities and control?

1. Capabilities define how long an AI Employee can stay awake, and control defines how it controls your devices.

2. Capabilities define what an AI Employee can access, and control define for how long it can access.

3. Capabilities define how an AI Employee can ask you first, and control defines how you steer it's response.

4. Capabilities define what an AI Employee can do, and control is what makes it Personal.

MCQ 6: What is the meaning of each component when mapped to the Agent OS analogy?

1. Gateway = Process memory, because they hold per-conversation context isolated per user, Workspace files = Device drivers, because these are capabilities loaded when needed, Plugins = Firmware, because these files go along with each request defining personality, identity and behavioral rules, Session = Kernel, because every request/response goes through this.

2. Gateway = Kernel, because every request/response goes through this, Workspace files = Firmware, because these files go along with each request defining personality, identity and behavioral rules, Plugins = Device drivers, because these are capabilities loaded when needed, Session = Process memory, because they hold per-conversation context isolated per user.

3. Gateway = Kernel, because kernel is the core part of a system, Workspace files = Firmware, because it's what software depends on, Plugins = Device drivers, because they work similar to mouse and keyboard, Session = Process memory, because each user wants to chat with their own requirements.

4. Gateway = Plugins, because every request/response goes through this, Workspace files = Kernel, because these files go along with each request defining personality, identity and behavioral rules, Plugins = Process memory, because these are capabilities loaded when needed, Session = Firmware, because they hold per-conversation context isolated per user.