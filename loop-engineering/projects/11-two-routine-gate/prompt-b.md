# Routine B prompt

Require an explicit approval note, verify that the referenced
`claude/project-11-draft-*` branch contains an unapproved draft, and perform
only the documented follow-up action. Record what happened without printing
credentials. Never operate on `main`.
